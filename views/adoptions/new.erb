<div>

  <div class="adoptions">
<table id="adoptions">

  <tr>
    <th></th>
    <th>Name</th>
    <th>Type</th>
    <th>Breed</th>
    <th>Adopters</th>
    <th>Animal Match</th>

  </tr>

  <% @animals.each do |animal| %>
  <tr>
    <td><img src="/images/adoption_animals/<%= animal.name %>.jpg"
              width="75" height="56" alt=""></td>

    <td><h4><a href="/adoptions/new<%= animal.id %>">
            <%= animal.name.capitalize %></a></h4></td>
        <td><h5><%= animal.type %></h5></td>
        <td><h5><%= animal.breed %></h5></td>
        <td>
          <form method="POST" action="/adoptions/new" >
          <!-- <label for="owner_id">New Owner</label> -->
            <select name="owner_id" id="owner_id">
              <% @owners.each do |owner| %>
                <option value="<%= owner.id %>"><%= owner.nice_name%></option>
              <% end %>
            <select> </td>
        <td>
          <!-- <label for="animal_id">Animal</label> -->
            <select name="animal_id" id="animal_id">
              <% @animals.each do |animal| %>
                <option value="<%= animal.id %>"><%= animal.name.capitalize%></option>
              <% end %>
            <select> <input type="submit" value="Submit Adoption"/>
          </td>
  </tr>
  <% end %>
  </table>
    </div>
</div>
    </form>

    <!-- all this needs refinement -->
